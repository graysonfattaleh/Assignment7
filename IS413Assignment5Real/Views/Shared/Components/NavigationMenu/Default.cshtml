
@model IEnumerable<string>
<a class="btn btn-block btn-outline-secondary small_guy" asp-action="Index"
   asp-controller="Home" asp-route-filterer="">Home</a>

@foreach (string category in Model)
{
    <a class="btn btn-block  small_guy
       @(category == @ViewBag.SelectedFilter ? "btn-primary" : "btn-outline-secondary" )"
       asp-action="Index"
       asp-controller="Home" asp-route-filterer="@category" asp-route-page="1">
        @category
    </a>
}